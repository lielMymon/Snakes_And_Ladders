export default class oneMove{
       constructor(num,ladder,goTo,snake,gold,dir,count){
              this.num=num;
              this.ladder=ladder;
              this.goTo=goTo;
              this.snake=snake;
              this.gold=gold;
              this.pic=[0,0];
              this.dir=dir;
              this.count=count;
       }
}